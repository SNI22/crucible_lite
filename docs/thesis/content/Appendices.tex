\chapter{Detailed Coverage and Supplementary Data}
\label{app:coverage}

\TODO{Populate with the final per-variant depth$\times$tilt coverage table
(cell counts for each of the six gripper variants across 0$^{\circ}$, x
$\pm2.5^{\circ}$, y $\pm2.5^{\circ}$) and any other supplementary per-trial
data tables referenced but not shown in full in Chapter~\ref{ch:results}.}

\TODO{Do NOT copy the approximate coverage numbers from
\texttt{docs/HANDOFF.md} Section 3.2 into this table --- that snapshot is
explicitly marked as approximate/stale ("re-derive fresh from filesystem
before making run decisions -- the matrix drifts fast"). Regenerate the
count table from \texttt{experiments/cloth\_grasp/} on disk immediately
before inserting it here.}

\section{Per-Variant Coverage Table}
\label{app:coverage-table}

\TODO{Insert regenerated table here.}
